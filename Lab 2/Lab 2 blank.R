#Setting the working directory
setwd("")

#Installing lidR and loading the library
install.packages("")
library()

#Installing rgl and loading the library
install.packages("")
library()

#creating a file path string for the .laz lidar file
LASfile <- ("")
#reading in the the LASfile with the readLAS function
las <- 
#plot the las file
plot()
#print the las file properties
print()
#print more details about the las file 
print()
#using las_check function to to see if the data conforms to las specifications

#plot the las file but specify the color as "Intensity"
plot(, color = "")
# plot the las file Intentsity but use quantile to adjust the color scale for outliers
  #You can play around with the number of breaks here
plot(, color = "", breaks = "", nbreaks = )
#plot the las file but with RGB colors
plot(, color = "")

